Primary CTA pill button in Public AI's brand red; use for the single main action (e.g. "TRY IT", "LOG IN").

```jsx
<Button onClick={go}>Try it</Button>
```

Labels render uppercase automatically. Hover shifts red #EF3C24 → #FE6550. For links out of the site use `ButtonLinkout` instead.
