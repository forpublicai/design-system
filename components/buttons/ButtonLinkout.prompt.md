Black pill button with a ↗ north-east arrow, for actions that leave the current site.

```jsx
<ButtonLinkout href="https://chat.publicai.co">Try it</ButtonLinkout>
```

Hover turns the fill gray (#929292). Same geometry as `Button` (48px pill, uppercase 14px).
