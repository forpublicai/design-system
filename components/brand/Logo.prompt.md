Official Public AI logo lockup (British Rail-inspired mark + wordmark). Never redraw; always use the SVG assets.

```jsx
<Logo height={40} />
<Logo variant="network" height={40} />
<LogoMark height={40} />
```

Pass `assetsBase` if the page is not at the design-system root (e.g. `assetsBase="../assets"`).

Related assets: `assets/public-ai-logo.svg`, `assets/public-ai-logo-mark.svg`, `assets/public-ai-network-logo.svg`, `assets/public-ai-inference-utility-logo.svg`.
