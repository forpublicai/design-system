Desktop/tablet top bar: plain-type wordmark on the left, red pill CTA on the right. No menu links.

```jsx
<Navigation title="Public AI" buttonLabel="LOG IN" onButtonClick={login} />
```

`breakpoint="tablet"` fixes width to 720px; desktop stretches to its container (max 1500px).
